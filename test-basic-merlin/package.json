{
 "name": "test-basic-merlin",
 "version": "0.1.0",
 "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "start": "env PORT=4000 concurrently \"gulp\" \"sleep 10 && start http://localhost:4000/graphql\""
 }
}