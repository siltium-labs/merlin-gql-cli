{
  "type": "{{database.type}}",
  "database": "{{database.name}}",
  "username": "{{database.user}}",
  "password": "{{database.password}}",
  "port": "{{database.port}}",
  "host": "{{database.host}}",
  "logging": ["query"],
  "synchronize": true,
  "entities": ["**/*.model.js"]
}
